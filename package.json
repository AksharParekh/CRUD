{
  "name": "crud-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "cd backend && npm install --production"
  },
  "dependencies": {
    "axios": "^1.14.0"
  }
}
